# KOHLER AI Bathroom Designer - Prototype dependencies
# Kept minimal for a 2-day build. Add packages only as later phases need them.

streamlit>=1.36.0
pandas>=2.2.0
numpy>=1.26.0
python-dotenv>=1.0.1

# Phase 3A: LLM requirement parser (src/llm_parser.py) uses the official
# Anthropic Python SDK to call the Claude API.
anthropic>=0.40.0
