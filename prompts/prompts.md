# Prompts

PHASE 1 STATUS: Placeholder. No prompts have been finalized yet.

This file will hold the system/user prompt templates used by
`src/llm_parser.py` to convert free-text bathroom requirements into
structured JSON (Phase 2), and later, prompts for conversational
"what-if" redesign (Phase 5+).

## Planned sections
- Requirement extraction prompt (text -> structured JSON)
- JSON schema the LLM must follow
- Few-shot examples
- What-if redesign / follow-up prompt
- Explanation-generation prompt ("why was this product selected")
